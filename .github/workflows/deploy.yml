name: Docs

on:
  workflow_run:
    workflows: ["Tests"]
    types:
      - completed

env:
  CARGO_TERM_COLOR: always

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3       
    - name: Install MDBook
      run: cargo install mdbook
